<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title><%= content_for?(:title) ? "Pelp | #{yield(:title)}" : "Pelp" %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= yield :styles %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= yield :scripts %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <div class="row row-separator hidden-xs"></div>
  <header class="row">
    <nav id="index-navbar" class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                  data-target="#index-navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "", root_path, class: "navbar-brand icon icon-pelp-logo" %>
        </div>
        <div class="collapse navbar-collapse" id="index-navbar-collapse">
          <ul class="nav navbar-nav navbar-left">
            <li><a href="#">Kursy</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="login.html">Zaloguj się</a></li>
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <section class="row">
    <%= bootstrap_flash %>
    <div class="jumbotron">
      <div class="container-fluid">
        <%= yield %>
      </div>
    </div>
  </section>
  <%= render 'layouts/footer' %>
</div>
</body>
</html>
